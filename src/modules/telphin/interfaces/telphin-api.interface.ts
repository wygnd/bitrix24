export interface TelphinTokenOptions {
  access_token: string;
  expires_in: number;
  token_type: string;
  scope: string;
}

export interface TelphinTokenData {
  accessToken: string;
  expiresIn: number;
}
